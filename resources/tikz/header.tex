\documentclass[beamer]{standalone}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{ifthen}
\newcommand\drawup[3]
{%
  \ifthenelse{\equal{#3}{align step}}{%
    \draw[->,#3] ($(#1,#2) - (0.15,0)$) -- ($(#1,#2) - (0.85,0)$);
  }{%
%    \draw[<-,erase step] ($(#1,#2) - (0.15,0)$) -- ($(#1,#2) - (0.85,0)$);
    \draw[->,#3] ($(#1,#2) - (0.15,0)$) -- ($(#1,#2) - (0.85,0)$);
  }
}
\newcommand\drawleft[3]
  {%
  \ifthenelse{\equal{#3}{align step}}{%
  \draw[->,#3] ($(#1,#2) - (0,0.15)$) -- ($(#1,#2) - (0,0.85)$);
  }{%
%  \draw[<-,erase step] ($(#1,#2) - (0,0.15)$) -- ($(#1,#2) - (0,0.85)$);
  \draw[->,#3] ($(#1,#2) - (0,0.15)$) -- ($(#1,#2) - (0,0.85)$);
  }
}

\newcommand\drawmatch[3]{%
  \ifthenelse{\equal{#3}{align step}}{%
    \draw[->,#3] ($(#1,#2) - (0.15,0.15)$) -- ($(#1,#2) - (0.85,0.85)$);
  }{%
%    \draw[<-,erase step] ($(#1,#2) - (0.15,0.15)$) -- ($(#1,#2) - (0.85,0.85)$);
    \draw[->,#3] ($(#1,#2) - (0.15,0.15)$) -- ($(#1,#2) - (0.85,0.85)$);
  }}

\tikzset{erase step/.style={>=latex,draw=white,fill=white,thick}}
\tikzset{align step/.style={>=latex,draw=black!30,fill=black!50,thick}}
\tikzset{path step/.style={>=latex,draw=black!50,fill=black!50,thick}}
\tikzset{optimal step/.style={>=latex,draw=black,fill=black,very thick}}
\tikzset{optimal-but-non-identical step/.style={>=latex,draw=red,fill=red,very thick}}
% Affine gap state styles — text nodes (no border, no fill background)
\tikzset{text-M/.style={text=blue!70}}
\tikzset{text-X/.style={text=green!60!black}}
\tikzset{text-Y/.style={text=orange!80!red}}
\tikzset{text-M-dim/.style={text=blue!30}}
\tikzset{text-X-dim/.style={text=green!25}}
\tikzset{text-Y-dim/.style={text=orange!30}}
% Affine gap state styles — arrows (test with simpler colors)
\tikzset{arrow-M/.style={>=latex,draw=blue,fill=blue,thick}}
\tikzset{arrow-X/.style={>=latex,draw=green,fill=green,thick}}
\tikzset{arrow-Y/.style={>=latex,draw=red,fill=red,thick}}
\tikzset{arrow-M-dim/.style={>=latex,draw=blue!30,fill=blue!30,thin}}
\tikzset{arrow-X-dim/.style={>=latex,draw=green!30,fill=green!30,thin}}
\tikzset{arrow-Y-dim/.style={>=latex,draw=red!30,fill=red!30,thin}}
% \begin{document}

% \begin{standaloneframe}
%   \begin{tikzpicture}[]
